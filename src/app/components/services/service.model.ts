import { Button, SectionList } from "../components.model";

export class Service {
    sectionList: SectionList[]=[];
    buttons: Button[]=[];
    header: string = "Service Header";
    statement: string = "The statement under the service header. A quick explanation of the service you are listing.";
}